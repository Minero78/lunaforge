import {NextRequest,NextResponse} from "next/server";import {publishNotification,getNotifications} from "@/lib/platform/notifications";export async function GET(request:NextRequest){const organizationId=request.nextUrl.searchParams.get("organizationId")??"";return NextResponse.json({notifications:getNotifications(organizationId)});}export async function POST(request:NextRequest){const body=await request.json();return NextResponse.json({notification:publishNotification(body)},{status:201});}