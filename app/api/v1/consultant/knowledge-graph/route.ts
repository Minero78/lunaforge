import {NextResponse} from "next/server";import {createSupabaseServerClient} from "@/lib/supabase/server";import {getOrganizationContext} from "@/lib/supabase/auth-context";import {buildKnowledgeGraph} from "@/lib/intelligence/knowledge-graph";export async function GET(){const c=await getOrganizationContext();const s=await createSupabaseServerClient();const [projects,actions,alerts]=await Promise.all([s.from("projects").select("*").eq("organization_id",c.organizationId),s.from("intervention_actions").select("*").eq("organization_id",c.organizationId),s.from("executive_alerts").select("*").eq("organization_id",c.organizationId)]);return NextResponse.json(buildKnowledgeGraph({projects:projects.data??[],actions:actions.data??[],alerts:alerts.data??[]}));}