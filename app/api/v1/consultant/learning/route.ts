import {NextResponse} from "next/server";import {createSupabaseServerClient} from "@/lib/supabase/server";import {getOrganizationContext} from "@/lib/supabase/auth-context";import {analyzeInterventionPatterns} from "@/lib/intelligence/intervention-patterns";import {buildRootCauseLibrary} from "@/lib/intelligence/root-cause-library";import {buildTransformationMemory} from "@/lib/intelligence/transformation-memory";export async function GET(){const c=await getOrganizationContext();const s=await createSupabaseServerClient();const {data,error}=await s.from("intervention_actions").select("*").eq("organization_id",c.organizationId);if(error)return NextResponse.json({error:error.message},{status:400});const actions=data??[];const patterns=analyzeInterventionPatterns(actions);const rootCauses=buildRootCauseLibrary(actions);return NextResponse.json({patterns,rootCauses,memory:buildTransformationMemory({patterns,rootCauses,actions})});}