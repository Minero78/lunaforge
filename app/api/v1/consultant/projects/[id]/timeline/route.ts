import {NextResponse} from "next/server";import {createSupabaseServerClient} from "@/lib/supabase/server";import {getOrganizationContext} from "@/lib/supabase/auth-context";import {buildProjectTimeline} from "@/lib/consulting/project-timeline";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const c=await getOrganizationContext();const s=await createSupabaseServerClient();const {data,error}=await s.from("portfolio_snapshots").select("snapshot_date,payload").eq("organization_id",c.organizationId).order("snapshot_date");if(error)throw error;return NextResponse.json({timeline:buildProjectTimeline(data??[],id)});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"TIMELINE_FAILED"},{status:400});}}