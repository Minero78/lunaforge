import {NextRequest,NextResponse} from "next/server";import {getDecisionAnalytics} from "@/lib/decision/analytics";import {generateDecisionRecommendation} from "@/lib/decision/recommendations";export async function GET(request:NextRequest){const organizationId=request.nextUrl.searchParams.get("organizationId");if(!organizationId)return NextResponse.json({error:"organizationId required"},{status:400});return NextResponse.json(await getDecisionAnalytics(organizationId));}export async function POST(request:NextRequest){const body=await request.json();return NextResponse.json(generateDecisionRecommendation(body));}