import {NextRequest,NextResponse} from "next/server";import {createDecision} from "@/lib/decision/engine";import {scoreDecision} from "@/lib/decision/scoring";import {requiresApproval} from "@/lib/decision/approval";import {canExecuteAutonomously} from "@/lib/decision/policies";import {validateDecisionGovernance} from "@/lib/decision/governance";export async function POST(request:NextRequest){const body=await request.json();const scoring=scoreDecision({confidence:body.confidence,impact:body.impact??50,urgency:body.urgency??50});let decision=createDecision({...body,confidence:scoring.confidence,risk:scoring.risk});decision={...decision,status:requiresApproval(decision)?"APPROVAL_REQUIRED":canExecuteAutonomously(decision)?"APPROVED":"PROPOSED"};return NextResponse.json({decision,scoring,governance:validateDecisionGovernance(decision)});} 