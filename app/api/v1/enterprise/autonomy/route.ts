import {NextRequest,NextResponse} from 'next/server';
import {orchestrateAgents,autonomousCommand,agentAnalytics,autonomyMetrics,autonomousExecutiveBrief} from '@/lib/autonomy';
export async function POST(request:NextRequest){const b=await request.json();const assignments=orchestrateAgents(b.agents??[],b.tasks??[]);const active=assignments.filter((x:any)=>x.agent).length;const command=autonomousCommand({agents:(b.agents??[]).length,activeTasks:active,risk:b.risk??0});const analytics=agentAnalytics({active,completed:b.completed??0,failed:b.failed??0});const metrics=autonomyMetrics({decisions:b.decisions??active,autonomous:active,interventions:b.interventions??0});return NextResponse.json({assignments,command,analytics,metrics,brief:autonomousExecutiveBrief({status:command.mode,risk:b.risk??0,actions:active}),generatedAt:new Date().toISOString()});}