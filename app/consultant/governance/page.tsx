"use client";import {useEffect,useState} from "react";import Link from "next/link";export default function GovernancePage(){const [events,setEvents]=useState<any[]>([]);useEffect(()=>{fetch("/api/v1/consultant/events").then(r=>r.json()).then(d=>setEvents(d.events??[]));},[]);return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto max-w-6xl"><Link href="/consultant/executive">← Executive Workspace</Link><h1 className="mt-4 text-3xl font-semibold">Governance Dashboard</h1><p className="mt-2 text-slate-600">Unified operational record of alerts, decisions and executive intelligence.</p><div className="mt-6 space-y-3">{events.map((e,i)=><article key={i} className="rounded-xl border bg-white p-4"><span className="text-xs font-semibold">{e.type}</span><p className="font-medium">{e.title}</p><p className="text-xs text-slate-500">{e.at}</p></article>)}</div></div></main>;}