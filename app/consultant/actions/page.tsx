"use client";import {useEffect,useState} from "react";import Link from "next/link";import {ActionAssignment} from "./ActionAssignment";const columns=["PROPOSED","APPROVED","IN_PROGRESS","COMPLETED"];export default function ActionsBoard(){const [actions,setActions]=useState<any[]>([]);useEffect(()=>{fetch("/api/v1/consultant/actions").then(r=>r.json()).then(d=>setActions(d.actions??[]));},[]);return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto max-w-7xl"><Link href="/consultant/operations">← Operations</Link><h1 className="mt-4 text-3xl font-semibold">Intervention Action Board</h1><div className="mt-6 grid gap-4 lg:grid-cols-4">{columns.map(col=><section key={col} className="rounded-xl bg-slate-100 p-3"><h2 className="text-sm font-semibold">{col}</h2><div className="mt-3 space-y-3">{actions.filter(a=>a.status===col).map(a=><article key={a.id} className="rounded-lg border bg-white p-3"><p className="text-xs">{a.severity}</p><h3 className="mt-1 font-medium">{a.title}</h3><ActionAssignment id={a.id} ownerId={a.owner_id}/></article>)}</div></section>)}</div></div></main>;}