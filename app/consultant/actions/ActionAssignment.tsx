"use client";import {useState} from "react";export function ActionAssignment({id,ownerId}:{id:string;ownerId?:string}){const [owner,setOwner]=useState(ownerId??"");async function save(){await fetch("/api/v1/consultant/actions/"+id,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({owner_id:owner||null})});}return <div className="mt-3 flex gap-2"><input value={owner} onChange={e=>setOwner(e.target.value)} placeholder="Owner UUID" className="rounded border px-2 py-1 text-sm text-slate-900"/><button onClick={save} className="rounded border px-3 py-1 text-sm">Assign</button></div>;}