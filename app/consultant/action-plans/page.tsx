"use client";import {useState} from "react";import Link from "next/link";export default function ActionPlans(){const [risk,setRisk]=useState("70");const [plan,setPlan]=useState<any>();async function generate(){const r=await fetch("/api/v1/consultant/action-plans",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:"manual",predictiveRiskScore:Number(risk)})});setPlan((await r.json()).plan);}return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto max-w-5xl"><Link href="/consultant/actions">← Actions</Link><h1 className="mt-4 text-3xl font-semibold">AI Suggested Action Plans</h1><div className="mt-5 flex gap-2"><input value={risk} onChange={e=>setRisk(e.target.value)} className="rounded border px-3"/><button onClick={generate} className="rounded bg-slate-900 px-4 py-2 text-white">Generate Plan</button></div>{plan&&<div className="mt-6 space-y-3">{plan.steps.map((s:any)=><article key={s.order} className="rounded-xl border bg-white p-4"><b>{s.order}. {s.title}</b><p>{s.description}</p></article>)}</div>}</div></main>;}