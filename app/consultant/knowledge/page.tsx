"use client";import {useEffect,useState} from "react";import Link from "next/link";export default function KnowledgeGraph(){const [d,setD]=useState<any>({nodes:[],edges:[]});useEffect(()=>{fetch("/api/v1/consultant/knowledge-graph").then(r=>r.json()).then(setD)},[]);return <main className="min-h-screen bg-slate-950 p-6 text-white"><div className="mx-auto max-w-6xl"><Link href="/consultant/learning">← Learning</Link><h1 className="mt-4 text-3xl font-semibold">Organizational Knowledge Graph</h1><p className="mt-2 text-slate-300">{d.nodes.length} entities · {d.edges.length} relationships</p><div className="mt-6 grid gap-3 md:grid-cols-2">{d.nodes.map((n:any)=><article key={n.id} className="rounded-xl border border-white/10 bg-white/5 p-4"><p className="text-xs text-indigo-300">{n.type}</p><p className="font-medium">{n.label}</p></article>)}</div></div></main>;}