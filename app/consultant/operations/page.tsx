"use client";import {useEffect,useState} from "react";import Link from "next/link";export default function OperationsCenter(){const [actions,setActions]=useState<any[]>([]);useEffect(()=>{fetch("/api/v1/consultant/actions").then(r=>r.json()).then(d=>setActions(d.actions??[]));},[]);return <main className="min-h-screen bg-slate-950 p-6 text-white"><div className="mx-auto max-w-7xl"><Link href="/consultant/command-center" className="text-sm text-slate-300">← Command Center</Link><h1 className="mt-4 text-4xl font-semibold">Autonomous Operations Center</h1><p className="mt-2 text-slate-300">Track intervention execution, deadlines, escalation and operational accountability.</p><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{actions.map(a=><article key={a.id} className="rounded-2xl border border-white/10 bg-white/5 p-5"><p className="text-xs text-indigo-300">{a.severity}</p><h2 className="mt-2 font-semibold">{a.title}</h2><p className="mt-2 text-sm text-slate-300">{a.status}</p><p className="mt-3 text-xs text-slate-400">Due: {a.due_at??"Not scheduled"}</p></article>)}</div></div></main>;}