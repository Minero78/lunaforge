"use client";import {useEffect,useState} from "react";import Link from "next/link";export default function Approvals(){const [actions,setActions]=useState<any[]>([]);useEffect(()=>{fetch("/api/v1/consultant/approvals").then(r=>r.json()).then(d=>setActions(d.actions??[]));},[]);async function decide(id:string,approval_status:string){await fetch("/api/v1/consultant/actions/"+id,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({approval_status,status:approval_status==="APPROVED"?"APPROVED":"PROPOSED"})});setActions(x=>x.filter(a=>a.id!==id));}return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto max-w-6xl"><Link href="/consultant/governance">← Governance</Link><h1 className="mt-4 text-3xl font-semibold">Executive Approval Center</h1><div className="mt-6 space-y-3">{actions.map(a=><article key={a.id} className="rounded-xl border bg-white p-5"><h2 className="font-semibold">{a.title}</h2><div className="mt-3 flex gap-2"><button onClick={()=>decide(a.id,"APPROVED")} className="rounded border px-3 py-2">Approve</button><button onClick={()=>decide(a.id,"REJECTED")} className="rounded border px-3 py-2">Reject</button></div></article>)}</div></div></main>;}