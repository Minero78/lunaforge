"use client";import {useEffect,useState} from "react";import Link from "next/link";export default function LearningDashboard(){const [d,setD]=useState<any>();useEffect(()=>{fetch("/api/v1/consultant/learning").then(r=>r.json()).then(setD)},[]);return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto max-w-6xl"><Link href="/consultant/execution">← Execution</Link><h1 className="mt-4 text-3xl font-semibold">Portfolio Learning Dashboard</h1><div className="mt-6 grid gap-4 md:grid-cols-3">{Object.entries(d?.memory??{}).filter(([k])=>k!=="version"&&k!=="updatedAt").map(([k,v])=><div key={k} className="rounded-xl border bg-white p-5"><p className="text-sm text-slate-500">{k}</p><p className="mt-2 text-2xl font-semibold">{String(v)}</p></div>)}</div><h2 className="mt-8 text-xl font-semibold">Intervention Patterns</h2><div className="mt-3 space-y-3">{(d?.patterns??[]).map((p:any)=><article key={p.pattern} className="rounded-xl border bg-white p-4"><b>{p.pattern}</b><p>{p.bestAction} · {p.averageEffectiveness}% effectiveness</p></article>)}</div></div></main>;}