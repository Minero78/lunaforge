"use client";import {useState} from "react";export function AlertActions({id,status}:{id:string;status:string}){const [current,setCurrent]=useState(status);async function update(next:string){const r=await fetch("/api/v1/consultant/alerts/"+id,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:next})});if(r.ok)setCurrent(next);}return <div className="mt-3 flex gap-2 text-xs">{current==="OPEN"&&<button onClick={()=>update("ACKNOWLEDGED")} className="rounded border px-3 py-2">Acknowledge</button>}{["OPEN","ACKNOWLEDGED"].includes(current)&&<button onClick={()=>update("IN_PROGRESS")} className="rounded border px-3 py-2">Start</button>}{current!=="RESOLVED"&&<button onClick={()=>update("RESOLVED")} className="rounded border px-3 py-2">Resolve</button>}<span className="self-center text-slate-500">{current}</span></div>;}