"use client";import {useEffect,useState} from "react";import Link from "next/link";
export default function AlertsPage(){const [alerts,setAlerts]=useState<any[]>([]);useEffect(()=>{fetch("/api/v1/consultant/alerts").then(r=>r.json()).then(d=>setAlerts(d.alerts??[]));},[]);return <main className="min-h-screen bg-slate-50 p-6 text-slate-950"><div className="mx-auto max-w-6xl"><Link href="/consultant/executive" className="text-sm">← Executive Workspace</Link><h1 className="mt-4 text-3xl font-semibold">Executive Notification Center</h1><div className="mt-6 space-y-3">{alerts.map(a=><article key={a.id} className="rounded-xl border bg-white p-5"><div className="flex justify-between gap-4"><div><p className="font-semibold">{a.title}</p><p className="mt-1 text-sm text-slate-600">{a.description}</p></div><div className="text-right text-xs"><p className="font-semibold">{a.severity}</p><p>{a.status}</p></div></div></article>)}{!alerts.length&&<p className="text-slate-500">No executive alerts.</p>}</div></div></main>;}