import {persistEvent} from "./persistent-events";import {DomainEvents} from "./domain-events";export async function emitProjectUpdate(input:{organizationId:string;projectId:string;changes:unknown}){return persistEvent({organizationId:input.organizationId,type:DomainEvents.PROJECT_UPDATED,payload:input});}export async function emitRiskChange(input:{organizationId:string;projectId:string;previousRisk:number;currentRisk:number}){return persistEvent({organizationId:input.organizationId,type:DomainEvents.PROJECT_RISK_CHANGED,payload:input});}