import {NextRequest,NextResponse} from "next/server";import {rateLimit} from "./rate-limit";export function guardApi(request:NextRequest,options:{limit?:number;windowMs?:number}={}){const key=request.headers.get("x-forwarded-for")??request.headers.get("x-request-id")??"anonymous";const result=rateLimit(key,options);if(!result.allowed)return NextResponse.json({error:"RATE_LIMITED",message:"Too many requests"},{status:429,headers:{"Retry-After":String(Math.ceil((result.resetAt-Date.now())/1000))}});return null;}