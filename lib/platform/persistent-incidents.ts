import {createSupabaseServerClient} from "@/lib/supabase/server";export async function createPersistentIncident(input:{organizationId:string;title:string;severity:string;metadata?:unknown}){const s=await createSupabaseServerClient();const {data,error}=await s.from("platform_incidents").insert({organization_id:input.organizationId,title:input.title,severity:input.severity,metadata:input.metadata??{}}).select().single();if(error)throw error;return data;}export async function listPersistentIncidents(organizationId:string){const s=await createSupabaseServerClient();const {data,error}=await s.from("platform_incidents").select("*").eq("organization_id",organizationId).order("created_at",{ascending:false});if(error)throw error;return data??[];}