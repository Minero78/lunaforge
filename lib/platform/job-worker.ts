import {claimNextJob} from "./persistent-jobs";import {withRetry} from "./retry";export async function runWorker(handler:(job:any)=>Promise<void>){const job=await claimNextJob();if(!job)return {processed:false};try{await withRetry(()=>handler(job));return {processed:true,jobId:job.id,status:"COMPLETED"};}catch(error){return {processed:true,jobId:job.id,status:"FAILED",error:error instanceof Error?error.message:"Unknown failure"};}}