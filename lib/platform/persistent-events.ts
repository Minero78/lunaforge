import {createSupabaseServerClient} from "@/lib/supabase/server";export async function persistEvent(input:{organizationId?:string;type:string;payload:unknown}){const s=await createSupabaseServerClient();const {data,error}=await s.from("platform_events").insert({organization_id:input.organizationId??null,type:input.type,payload:input.payload}).select().single();if(error)throw error;return data;}export async function getPendingEvents(limit=20){const s=await createSupabaseServerClient();const {data,error}=await s.from("platform_events").select("*").eq("status","PENDING").order("created_at").limit(limit);if(error)throw error;return data??[];}