import {createSupabaseServerClient} from "@/lib/supabase/server";export async function persistAuditEvent(input:{organizationId?:string;actorId?:string;action:string;resource:string;resourceId?:string;metadata?:unknown}){const s=await createSupabaseServerClient();const {data,error}=await s.from("platform_audit_events").insert({organization_id:input.organizationId??null,actor_id:input.actorId??null,action:input.action,resource:input.resource,resource_id:input.resourceId??null,metadata:input.metadata??{}}).select().single();if(error)throw error;return data;}