import {createSupabaseServerClient} from "@/lib/supabase/server";export async function createPersistentNotification(input:{organizationId:string;type:string;title:string;message:string}){const s=await createSupabaseServerClient();const {data,error}=await s.from("platform_notifications").insert({organization_id:input.organizationId,type:input.type,title:input.title,message:input.message}).select().single();if(error)throw error;return data;}export async function listPersistentNotifications(organizationId:string){const s=await createSupabaseServerClient();const {data,error}=await s.from("platform_notifications").select("*").eq("organization_id",organizationId).order("created_at",{ascending:false});if(error)throw error;return data??[];}