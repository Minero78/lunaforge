import {signWebhook} from "./webhooks";export async function deliverWebhook(input:{url:string;payload:unknown;secret:string}){const body=JSON.stringify(input.payload);const signature=signWebhook(body,input.secret);const response=await fetch(input.url,{method:"POST",headers:{"content-type":"application/json","x-lunaforge-signature":signature},body});if(!response.ok)throw new Error("Webhook delivery failed: "+response.status);return {status:response.status,delivered:true};}