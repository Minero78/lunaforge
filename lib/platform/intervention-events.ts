import {persistEvent} from "./persistent-events";import {DomainEvents} from "./domain-events";export async function interventionStarted(input:{organizationId:string;interventionId:string;projectId:string}){return persistEvent({organizationId:input.organizationId,type:DomainEvents.INTERVENTION_STARTED,payload:input});}export async function interventionCompleted(input:{organizationId:string;interventionId:string;projectId:string;outcome:unknown}){return persistEvent({organizationId:input.organizationId,type:DomainEvents.INTERVENTION_COMPLETED,payload:input});}