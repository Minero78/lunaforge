export function benchmarkOrganization(metrics:any[],current:any){const peers=metrics.filter(x=>x.organizationId!==current.organizationId);const avg=(key:string)=>peers.length?peers.reduce((n,x)=>n+Number(x[key]??0),0)/peers.length:0;const dimensions=["health","delivery","risk","effectiveness"];const benchmarks=Object.fromEntries(dimensions.map(k=>[k,{organization:Number(current[k]??0),peerAverage:Math.round(avg(k)),delta:Math.round(Number(current[k]??0)-avg(k))}]));return {organizationId:current.organizationId,benchmarks};}