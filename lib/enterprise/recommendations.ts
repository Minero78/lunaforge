export function generateCommandRecommendations(input:{risk:number;capacity:number;criticalProjects:number}){const recommendations:string[]=[];if(input.risk>=75)recommendations.push("Escalate strategic risk and launch mitigation review.");if(input.capacity<30)recommendations.push("Rebalance constrained resources across critical projects.");if(input.criticalProjects>0)recommendations.push("Prioritize executive review for critical projects.");if(!recommendations.length)recommendations.push("Maintain current operating strategy and continue monitoring.");return recommendations;}