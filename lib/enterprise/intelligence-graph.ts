export type GraphNode={id:string;type:string;label:string;metadata?:Record<string,unknown>};export type GraphEdge={from:string;to:string;type:string;weight?:number};export function buildIntelligenceGraph(nodes:GraphNode[],edges:GraphEdge[]){const adjacency=Object.fromEntries(nodes.map(n=>[n.id,edges.filter(e=>e.from===n.id||e.to===n.id)]));return {nodes,edges,adjacency,nodeCount:nodes.length,edgeCount:edges.length};}