export function learnFromInterventions(actions:any[]){const completed=actions.filter(a=>a.status==="COMPLETED"&&typeof a.effectiveness_score==="number");const average=completed.length?completed.reduce((n,a)=>n+a.effectiveness_score,0)/completed.length:0;const bySeverity=Object.fromEntries(["LOW","MEDIUM","HIGH","CRITICAL"].map(s=>{const x=completed.filter(a=>a.severity===s);return [s,x.length?x.reduce((n,a)=>n+a.effectiveness_score,0)/x.length:0]}));return {sampleSize:completed.length,averageEffectiveness:Math.round(average),bySeverity};}