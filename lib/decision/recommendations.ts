import {scoreDecision} from "./scoring";export function generateDecisionRecommendation(input:{signal:string;confidence:number;impact:number;urgency:number}){const scoring=scoreDecision(input);const priority=scoring.risk==="CRITICAL"?"Immediate executive intervention":scoring.risk==="HIGH"?"Prioritized mitigation":"Monitor and optimize";return {signal:input.signal,...scoring,recommendation:priority,rationale:`Recommendation generated from confidence ${scoring.confidence}% and exposure ${scoring.exposure}.`};}