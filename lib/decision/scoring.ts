import type {DecisionRisk} from "./types";export function scoreDecision(input:{confidence:number;impact:number;urgency:number}){const confidence=Math.max(0,Math.min(100,input.confidence));const exposure=Math.max(0,Math.min(100,(input.impact+input.urgency)/2));const risk:DecisionRisk=exposure>=85?"CRITICAL":exposure>=65?"HIGH":exposure>=35?"MEDIUM":"LOW";return {confidence,risk,exposure};}