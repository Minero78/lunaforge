import {getOutcomes} from "./outcomes";export function learnFromDecision(decisionId:string){const outcomes=getOutcomes(decisionId);if(!outcomes.length)return {decisionId,learning:"INSUFFICIENT_DATA"};const successRate=outcomes.filter(x=>x.success).length/outcomes.length;const averageImpact=outcomes.reduce((n,x)=>n+x.impact,0)/outcomes.length;return {decisionId,learning:"UPDATED",successRate,averageImpact};}