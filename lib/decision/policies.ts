import type {Decision} from "./types";export type ActionPolicy={allowAutonomous:boolean;minConfidence:number;blockedRisks:string[]};export const defaultPolicy:ActionPolicy={allowAutonomous:true,minConfidence:85,blockedRisks:["HIGH","CRITICAL"]};export function canExecuteAutonomously(decision:Decision,policy=defaultPolicy){return policy.allowAutonomous&&decision.confidence>=policy.minConfidence&&!policy.blockedRisks.includes(decision.risk);}