export type DecisionOutcome={decisionId:string;success:boolean;impact:number;notes?:string;measuredAt:string};const outcomes:DecisionOutcome[]=[];export function recordOutcome(input:Omit<DecisionOutcome,"measuredAt">){const outcome={...input,measuredAt:new Date().toISOString()};outcomes.push(outcome);return outcome;}export function getOutcomes(decisionId:string){return outcomes.filter(x=>x.decisionId===decisionId);}