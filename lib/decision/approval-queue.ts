import {createSupabaseServerClient} from "@/lib/supabase/server";export async function enqueueApproval(input:{decisionId:string;organizationId:string}){const s=await createSupabaseServerClient();const {data,error}=await s.from("platform_decision_approvals").insert({decision_id:input.decisionId,organization_id:input.organizationId}).select().single();if(error)throw error;return data;}export async function getApprovalQueue(organizationId:string){const s=await createSupabaseServerClient();const {data,error}=await s.from("platform_decision_approvals").select("*,platform_decisions(title,risk,confidence,recommendation)").eq("organization_id",organizationId).eq("status","PENDING").order("requested_at");if(error)throw error;return data??[];}