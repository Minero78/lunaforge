import type {Decision,DecisionRisk} from "./types";export function createDecision(input:{organizationId:string;projectId?:string;type:string;title:string;confidence:number;recommendation:string;explanation:string;risk:DecisionRisk}):Decision{return{id:crypto.randomUUID(),organizationId:input.organizationId,projectId:input.projectId,type:input.type,title:input.title,risk:input.risk,confidence:Math.max(0,Math.min(100,input.confidence)),status:"PROPOSED",recommendation:input.recommendation,explanation:input.explanation,createdAt:new Date().toISOString()};}