import {createSupabaseServerClient} from "@/lib/supabase/server";export async function persistDecisionOutcome(input:{decisionId:string;success:boolean;impact:number;notes?:string}){const s=await createSupabaseServerClient();const {data,error}=await s.from("platform_decision_outcomes").insert({decision_id:input.decisionId,success:input.success,impact:input.impact,notes:input.notes??null}).select().single();if(error)throw error;return data;}