export function analyzeScenario(project:any,scenario:"NO_ACTION"|"INTERVENE"){const risk=project.predictiveRiskScore??50;if(scenario==="NO_ACTION")return {scenario,project:project.name,expectedRisk:Math.min(100,risk+Math.max(5,Math.round(risk*.15))),message:"Without intervention, deterioration risk is expected to remain elevated."};return {scenario,project:project.name,expectedRisk:Math.max(0,risk-Math.max(5,Math.round(risk*.2))),message:"Timely intervention creates a plausible path to risk reduction; actual impact must be measured."};}