export function buildKnowledgeGraph(input:{projects:any[];actions:any[];alerts:any[]}){const nodes=[...input.projects.map(p=>({id:"project:"+p.id,type:"PROJECT",label:p.name})),...input.actions.map(a=>({id:"action:"+a.id,type:"ACTION",label:a.title})),...input.alerts.map(a=>({id:"alert:"+a.id,type:"RISK",label:a.title}))];const edges=[...input.actions.filter(a=>a.project_id).map(a=>({from:"project:"+a.project_id,to:"action:"+a.id,type:"HAS_ACTION"})),...input.alerts.filter(a=>a.project_id).map(a=>({from:"project:"+a.project_id,to:"alert:"+a.id,type:"HAS_RISK"}))];return {nodes,edges};}