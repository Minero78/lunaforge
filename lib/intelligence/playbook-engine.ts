export function generatePlaybook(rootCause:string,patterns:any[]){const p=patterns.find(x=>x.pattern===rootCause);return {rootCause,playbookName:`${rootCause} Recovery Playbook`,recommendedAction:p?.bestAction??"Validate root cause",steps:["Confirm evidence","Assign accountable owner","Execute intervention","Measure effectiveness","Capture learning"],historicalEffectiveness:p?.averageEffectiveness??null};}