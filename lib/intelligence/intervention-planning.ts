export function createInterventionPlan(input:{decisionId:string;objective:string;actions:{name:string;owner:string;etaHours:number}[]}){const ordered=[...input.actions].sort((a,b)=>a.etaHours-b.etaHours);return {planId:crypto.randomUUID(),decisionId:input.decisionId,objective:input.objective,actions:ordered,totalEstimatedHours:ordered.reduce((n,a)=>n+a.etaHours,0),createdAt:new Date().toISOString()};}