export function forecastDecisionImpact(input:{baseline:number;expectedImprovement:number;confidence:number;timeHorizonDays:number}){const expected=input.baseline+input.expectedImprovement;const uncertainty=(100-input.confidence)/100*Math.abs(input.expectedImprovement);return {baseline:input.baseline,expected,lowerBound:expected-uncertainty,upperBound:expected+uncertainty,timeHorizonDays:input.timeHorizonDays};}