import type {EnterpriseAgent,AgentTask} from './types';export function routeTask(agents:EnterpriseAgent[],task:AgentTask){return [...agents].filter(a=>a.status==='IDLE'||a.status==='PLANNING').sort((a,b)=>(b.capabilities.filter(c=>task.goal.toLowerCase().includes(c.toLowerCase())).length+b.trust+b.reputation)-(a.capabilities.filter(c=>task.goal.toLowerCase().includes(c.toLowerCase())).length+a.trust+a.reputation))[0]??null;}